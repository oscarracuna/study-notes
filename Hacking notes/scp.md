
Used to transfer files to your local machine when connected to SSH

```shell
scp user@localmachineip:filename location/to/transfer/to
```

You can also transfer from local to ssh:
```shell
scp /path/to/linpeas.sh user@target-ip:/tmp/
```


[[SSH]]