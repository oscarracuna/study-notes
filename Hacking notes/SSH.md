To connect via SSH, run the following command:
``` bash
ssh username@[IP or domain]
```

You can download stuff from SSH into your local machine via [[scp]]

```shell
scp username@ip:/path/to/file /local/path/to/file
```
